<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>rubnyemile</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Georgia', serif;
            background-color: #fafaf8;
            color: #2c2c2c;
            line-height: 1.8;
        }

        .container {
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar */
        .sidebar {
            width: 2.5in;
            background-color: #d4c5b9;
            padding: 40px 30px;
            position: fixed;
            height: 100vh;
            overflow-y: auto;
            border-right: 2px solid #7a9d6f;
        }

        .sidebar h1 {
            font-size: 24px;
            margin-bottom: 30px;
            color: #2c2c2c;
            font-weight: normal;
            letter-spacing: 1px;
        }

        .nav-section {
            margin-bottom: 25px;
        }

        .nav-title {
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: #5c5c5c;
            margin-bottom: 12px;
            cursor: pointer;
            display: flex;
            justify-content: space-between;
